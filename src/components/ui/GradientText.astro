---
import { twMerge } from 'tailwind-merge';

const {
  style2 = false,
  text = Astro.slots.render('default'),
  class: className = '',
  isHeading = false,
} = Astro.props as {
  style2?: boolean;
  text: string;
  class?: string;
  isHeading?: boolean;
};
---

<span
  class={twMerge(
    'text-primary highlight inline-block',
    style2 && 'bg-gradient-to-r from-orange-600 to-yellow-400 text-transparent bg-clip-text italic',
    className,
    isHeading &&
      'text-5xl md:text-6xl xl:text-[60px] xl:leading-[64px] font-bold leading-tighter tracking-tighter mb-4 xl:mb-[16px] font-heading'
  )}>{text}</span
>
